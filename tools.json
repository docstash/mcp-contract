{
  "tools": [
    {
      "name": "list_documents",
      "description": "List the user's documents. By default returns ACTIVE docs in their default organization (team org if they have one, personal otherwise). Use `which` to switch lens:\n  • 'active'         — default. Docs the user owns or is an org member on. Has the user's own work.\n  • 'trash'          — recently-trashed docs. Use when the user says \"what did I delete\" / \"restore X\" / \"undelete\".\n  • 'shared-with-me' — docs OTHER people shared with this user (cross-org grants).\n\nReturns slug, name, url, type, language, owner attribution, and latest-version summary for each. Use this to find an existing doc's `slug` before calling `read_document`, `manage_document`, `manage_sharing`, or an authoring tool.",
      "annotations": {
        "title": "List documents",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "which": {
            "type": "string",
            "enum": [
              "active",
              "trash",
              "shared-with-me"
            ],
            "description": "Which list to return. Default: 'active'."
          }
        },
        "additionalProperties": true
      }
    },
    {
      "name": "show_pdf",
      "description": "DISPLAY a PDF to the user inline by slug — renders OFF your context (bytes go to the widget, nothing enters the conversation; returns metadata + an artifact reference, never the text). Use when the user asks to SEE a doc (\"show / open / pull up X\"). To load the TEXT into your context to summarize or revise, use `read_document` instead. `show_pdf` shows only a PDF; a wrong-type call names the right tool.",
      "annotations": {
        "title": "Show PDF",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug (from `stash` / `list_documents` / URLs)."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "show_docx",
      "description": "DISPLAY a Word (.docx) doc to the user inline by slug — renders OFF your context (bytes go to the widget, nothing enters the conversation; returns metadata + an artifact reference, never the text). Use when the user asks to SEE a doc (\"show / open / pull up X\"). To load the TEXT into your context to summarize or revise, use `read_document` instead. `show_docx` shows only a Word (.docx) doc; a wrong-type call names the right tool.",
      "annotations": {
        "title": "Show Word doc",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug (from `stash` / `list_documents` / URLs)."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "show_sheet",
      "description": "DISPLAY a spreadsheet to the user inline by slug — renders OFF your context (bytes go to the widget, nothing enters the conversation; returns metadata + an artifact reference, never the text). Use when the user asks to SEE a doc (\"show / open / pull up X\"). To load the TEXT into your context to summarize or revise, use `read_document` instead. `show_sheet` shows only a spreadsheet; a wrong-type call names the right tool.",
      "annotations": {
        "title": "Show spreadsheet",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug (from `stash` / `list_documents` / URLs)."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "show_page",
      "description": "DISPLAY a web page to the user inline by slug — renders OFF your context (bytes go to the widget, nothing enters the conversation; returns metadata + an artifact reference, never the text). Use when the user asks to SEE a doc (\"show / open / pull up X\"). To load the TEXT into your context to summarize or revise, use `read_document` instead. `show_page` shows only a web page; a wrong-type call names the right tool.",
      "annotations": {
        "title": "Show web page",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug (from `stash` / `list_documents` / URLs)."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "show_text",
      "description": "DISPLAY a markdown / text / code doc to the user inline by slug — renders OFF your context (bytes go to the widget, nothing enters the conversation; returns metadata + an artifact reference, never the text). Use when the user asks to SEE a doc (\"show / open / pull up X\"). To load the TEXT into your context to summarize or revise, use `read_document` instead. `show_text` shows only a markdown / text / code doc; a wrong-type call names the right tool.",
      "annotations": {
        "title": "Show text or code",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug (from `stash` / `list_documents` / URLs)."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "read_document",
      "description": "Read a document's CONTENT into your context by slug — a SILENT read: NOTHING is rendered or shown to the user (no widget, no preview, no link card). This is the right FIRST call whenever you need the text to reason over — to summarize it, answer questions about it, or change it (then apply the change with the matching `edit_*` tool — edit_pdf / edit_docx / edit_sheet / edit_page / edit_text / edit_app — as surgical `{ old_string → new_string }` edits, same slug; for a docx or sheet, match the VISIBLE text/value and DocStash handles the JSON escaping).\n\nReturns the doc's metadata, a reference to its latest artifact (id, signed fileUrl, metadata), `content` — the full body text — for text-like kinds (html/md/text + agent-authored pdf print-HTML + the xlsx/docx specs; binary uploads aren't text-extractable), and any unresolved lint `errors` on the latest version (this is ALSO the way to re-check errors mid-fix-loop — no separate errors tool).\n\nPass `withHistory: true` to also include the activity feed — every event newest-first (type, timestamp, author, and for content_pushed events the `artifactId` used by manage_document action='revert'). To DISPLAY the doc to the user instead, call the matching `show_*` tool (show_pdf / show_page / show_sheet / …) — it renders inline entirely off your context.\n\nMULTI-FILE APPS: a bare read returns the bundle's file paths in `files` (no single body). Pass `file` (a path from that list) to read ONE file's contents into `content` — then change it surgically with `edit_app` (same `file`).",
      "annotations": {
        "title": "Read document",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug (from `stash` / `list_documents` / URLs)."
          },
          "withHistory": {
            "type": "boolean",
            "description": "If true, also include the activity history (event feed with artifact ids) in the response."
          },
          "file": {
            "type": "string",
            "description": "APP ONLY: a bundle file path (from the `files` list a bare read returns) to read that one file's contents into `content`."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "screenshot_document",
      "description": "SCREENSHOT a staged document by slug — the rendered result comes back AS IMAGES for your own review, plus the deterministic `errors`. Renders the latest pushed version and returns:\n  • `errors` — problems detected at render time. PDF: freeflow checks (content wider than the page can hold — an unbreakable string or too-wide table; a keep-whole block taller than one page; text buried under other text; a blank page mid-document; a broken image; a blank document). HTML/APP: viewport + runtime checks (horizontal blowout on desktop or mobile, missing viewport meta, broken images, a blank render, uncaught boot exceptions, missing bundle files, console errors). ALL advisory — nothing blocks a stash; fix them when iterating, never by stripping design.\n  • screenshots delivered to YOU as image content — PDF: one image per page at `resolution` px wide (DEFAULT 500 — enough to judge layout / overflow / colour and cheap on tokens; raise to ~1000–1600 ONLY to read fine print), up to 8 pages per call. A bare call serves pages 1-8; pass `firstPage`/`lastPage` for a contiguous range, or `pages` for an EXPLICIT set like [2,5] so pages you did not touch never enter your context. When more pages remain than one call ships, the result names what shipped (`pageRange`) — call again for the rest. Everything else: a bounded full-height shot at DESKTOP and at MOBILE (390px) — so you can SEE the result and catch what the checks cannot: hidden text, wrong colours, a broken visual layout.\n\nCall it after authoring or iterating a PDF, page, or app to check your work BEFORE telling the user it is ready — especially when a create tool reported errors. The images return to YOU (the model); they are NOT the user-facing inline widget.",
      "annotations": {
        "title": "Screenshot document",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug (from a create_* result / list_documents / a docstash URL)."
          },
          "firstPage": {
            "type": "integer",
            "minimum": 1,
            "description": "PDF only, optional: 1-based first page of the range to screenshot — use when specific pages need review (the user says \"pages 5-8 are broken\") or to fetch the next window of a longer doc. Omit for the first 8 pages."
          },
          "lastPage": {
            "type": "integer",
            "minimum": 1,
            "description": "PDF only, optional: 1-based last page of the explicit range (defaults to firstPage — a single page). At most 8 pages ship per call."
          },
          "pages": {
            "type": "array",
            "items": {
              "type": "integer",
              "minimum": 1
            },
            "maxItems": 8,
            "description": "PDF only, optional: EXPLICIT 1-based page numbers to screenshot, e.g. [2,5] — a discontiguous set. Use this after editing specific pages so untouched pages never enter your context. Overrides firstPage/lastPage. Max 8."
          },
          "resolution": {
            "type": "integer",
            "minimum": 256,
            "maximum": 1600,
            "description": "PDF only, optional: width in px of each page image. DEFAULT 500 — enough to judge layout, overflow, colour, and composition, and cheap on tokens (image cost scales with area, and these screenshots are your biggest token cost). Raise it (≈1000–1600) ONLY when you must read fine print. Prefer the smallest resolution that answers your question."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "list_org_members",
      "description": "List members of the user's default organization — their names AND emails. Use this to figure out WHO TO SHARE WITH when the user says \"share with the team\" / \"who's on this organization?\" / \"share with Alice\" (and you need to resolve Alice → email).\n\nReturns nothing useful for a personal organization (just the user themselves). For team organizations returns the full member roster.\n\nUse the emails returned here as the `email` argument to `manage_sharing` action='add'.",
      "annotations": {
        "title": "List organization members",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": true
      }
    },
    {
      "name": "manage_document",
      "description": "Doc-level actions other than upload. Pass the `slug` plus an `action`:\n\n  • 'rename'  — change the doc's name. Requires `name`. No new version minted (metadata only). Use when the user says \"rename X to Y\".\n  • 'trash'   — soft-delete. Doc disappears from active list, shows up in `list_documents` with which='trash'. RECOVERABLE — don't tell the user it's permanently gone.\n  • 'restore' — un-trash. Mirror of 'trash'. Find the slug via `list_documents` with which='trash'.\n  • 'revert'  — repoint the doc at an EARLIER artifact. Requires `artifactId` (get it from `read_document` with withHistory:true — each content_pushed event carries `payload.artifactId`). No bytes move; the server bumps that artifact's refCount and logs a content_pushed event. History stays intact.\n\nFor UPLOADING content (create a new doc, push a new version), use the separate `stash` tool — NOT this one.",
      "annotations": {
        "title": "Manage document",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug."
          },
          "action": {
            "type": "string",
            "enum": [
              "rename",
              "trash",
              "restore",
              "revert"
            ],
            "description": "Which action to perform on the doc."
          },
          "name": {
            "type": "string",
            "description": "Required when action='rename'. The new name (up to 200 chars)."
          },
          "artifactId": {
            "type": "string",
            "description": "Required when action='revert'. The full id of the artifact to revert to (from `read_document` with withHistory:true)."
          }
        },
        "required": [
          "slug",
          "action"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "manage_sharing",
      "description": "Share AND PUBLISH a doc — this is the ONLY tool that makes a doc public. Use it to PUBLISH / make public / get a public shareable link (action='set-public', isPublic=true), and to grant/revoke per-user access. Pass `slug` + `action`:\n\n  • 'list'        — list all per-user grants (Viewers + Editors). Does NOT include public-link state (use `read_document` for that — it's on `document.isPublic`).\n  • 'add'         — grant a specific person access. Requires `email`. Optional `level: 'read' | 'write'` (default 'read'). The recipient must be a DocStash user (sign in once at app.docstash.ai). NOT a public link.\n  • 'revoke'      — revoke a per-user grant by `email`. Mirror of 'add'.\n  • 'set-public'  — PUBLISH / unpublish the doc: toggle its PUBLIC LINK. Requires `isPublic: boolean`. true = PUBLISH — anyone with the URL sees the latest version (anonymous, no sign-in); false = revoke the public link. This is the ONLY way to make a doc publicly viewable — a bare stashed url stays private. The doc must be saved (stashed) first; you can't publish an unsaved version. Per-doc only — no version-pinned public links.\n\nCall this when the user explicitly asks to PUBLISH, make public, get a public/shareable link, or share/revoke per-user access. Don't infer sharing from vague phrasing like \"can you give Alice access\" without checking what level they want.",
      "annotations": {
        "title": "Manage sharing",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The doc's 6-char slug."
          },
          "action": {
            "type": "string",
            "enum": [
              "list",
              "add",
              "revoke",
              "set-public"
            ],
            "description": "Which share operation."
          },
          "email": {
            "type": "string",
            "description": "Required when action='add' or 'revoke'. The grantee's email."
          },
          "level": {
            "type": "string",
            "enum": [
              "read",
              "write"
            ],
            "description": "Optional, only meaningful when action='add'. 'read' (Viewer) or 'write' (Editor). Default 'read'."
          },
          "isPublic": {
            "type": "boolean",
            "description": "Required when action='set-public'. true = enable public link; false = revoke."
          }
        },
        "required": [
          "slug",
          "action"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "create_pdf",
      "description": "Generate a beautiful, print-ready PDF document for DocStash and show the user an INLINE PREVIEW: reports, memos, proposals, letters, résumés, whitepapers, contracts, essays, one-pagers. THIS IS THE DEFAULT for a generic 'make me a document' / 'make me a PDF' request (use `create_docx` only when the user specifically wants an editable WORD doc, `create_text` for markdown/code source, `create_page` for a web page). `content` = HTML you author, and DocStash renders + presents it AS A PDF (paginated paper, page nav, zoom, one-click PDF download). Do NOT generate a binary .pdf; the user drag-and-drops pre-existing .pdf files on the dashboard.\n\nThis is a REAL HTML/CSS document rendered by a full Chromium engine and baked to PDF — you have the SAME visual toolkit a web page has (any CSS layout, SVG, gradients, filters, transforms, web fonts, images), NOT a limited document format. It is a STATIC print document: NO `<script>` / JS runs (it is rejected) — draw every chart, diagram and graphic as hand-authored inline SVG + CSS (crisp, and how the best docs are built). The ONLY difference from a live web page is GEOMETRY: it prints onto fixed US Letter pages that never scroll or reflow.\n\nPAGE MODEL — TWO ways to build a page, mix them freely in one document:\n  1. FLOW (default) — author continuous, top-to-bottom HTML (US Letter, 816x1056px @96dpi). You do NOT emit per-page blocks or decide where breaks fall: DocStash flows your content across as many pages as it needs and paginates for you (keeping headings with their text; not splitting tables / figures / cards). Best for running text, long tables, card lists — anything whose length you can't predict.\n  2. CANVAS — `<section class=\"pdf-canvas\">` is a FIXED one-page design surface: exactly one sheet, edge-to-edge, a positioning context. Inside it you CAN and SHOULD absolutely-position art, full-page backgrounds, and edge-anchored elements against KNOWN bounds — compose it like a POSTER. Its content auto-fits to one page at bake, so design ~a page without measuring height. Use a canvas for the set-pieces that make a doc look designed: the COVER, section dividers, full-page infographics / charts / hero spreads.\n  Opt-in controls anywhere: `<div class=\"pdf-break\"></div>` forces a fresh page; `pdf-keep` keeps a card / figure / callout from splitting; `<section class=\"pdf-bleed\">` is a full-bleed page that still FLOWS (for a themed section whose background reaches the edges but whose length is unknown).\n\nDESIGN THE DOCUMENT:\n  • Make page 1 an attractive COVER — a `pdf-canvas`: absolutely-position the title, a full-bleed background, and any art against the fixed page, the way a poster is composed. Reach for a canvas again for section dividers and full-page infographics.\n  • VISUALIZE — show as much as you tell, all as hand-authored inline SVG (+ CSS): charts (bar/line/donut via `stroke-dasharray`), timelines, diagrams, stat panels, maps, iconography, a signature motif derived from the subject. This is what makes a doc look designed — and it is exactly how the best docs are built (no JS needed).\n  • RUNNING HEADER / FOOTER (optional) — put page numbers, a title strip, or a small logo on every page via CSS `@page` margin boxes, in ANY edge or corner (`@top-left`, `@bottom-center`, `@top-right-corner`, `@left-middle`, …), styled however fits. Fill it with the page counters (`counter(page)`, `counter(page) \" / \" counter(pages)`, `counter(page, decimal-leading-zero)`), a running `string-set` section title, or a `position:running()` element (e.g. a logo). Omit it entirely for a résumé / invite / one-pager. Cover, `pdf-canvas`, and full-bleed pages are kept clear of it automatically.\n  • TYPE SCALE — 816px wide = US Letter 8.5in, so **1 CSS px = 0.75pt**: `11px` body copy PRINTS AT 8.25pt. Size in px but think in pt — body copy ~13-15px (~10-11pt), never below 11px for anything a reader must read; 9px (~7pt) is the floor for incidental labels / footers.\n  • FILL each page — a sheet is a WHOLE page whether or not its content fills it, so a half-empty interior page reads as a mistake, not restraint. Pace and size content (type scale, spacing, columns) so every page is ROUGHLY FULL; only the FINAL page should run partial, and never orphan a lone heading or a few trailing lines. Balanced, full pages are what separate a designed document from an exported one.\n  • MARGINS — the engine ALREADY applies a comfortable page margin (0.7in) so text never runs to the paper edge; do NOT add your own outer page margin or wrap pages in your own padding on top of it (that double-margins the page). To bleed to the edge on purpose (cover, background, edge-to-edge image), use `pdf-bleed` — it zeroes the margin for that page.\n  • To carry a color / dark theme edge-to-edge across EVERY page (not just the cover), set it on the page background — `@page{background:...}` + a matching `body` color. In FLOW content do NOT pin heights or absolutely-position a page layout (no `height:11in`, no `100vh` — the engine owns flow pagination); absolute positioning and a fixed page box are for INSIDE a `pdf-canvas`, where they are the whole point.\n  • Self-contained: inline all `<style>`; load fonts / images / libraries from absolute https/CDN URLs. IMAGES & FONTS — image/font URLs MUST be DIRECT files (a gallery/viewer page renders BROKEN); the direct-URL recipes, sizing, and custom-font rules are in the server instructions (VISUAL DOCUMENTS & MEDIA). REPEATING gradient patterns (`repeating-linear-gradient`, tightly-tiled backgrounds) rasterize unreliably in print — build stripes / hatching from REAL repeated elements (divs / spans) instead.\n\nNothing is stashed yet — then STOP: do NOT call `stash` yourself; the user reviews the preview and saves it. Full lifecycle, slug/versioning, attribution + publish rules: DocStash server instructions.",
      "annotations": {
        "title": "Create PDF",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Required when creating a new doc; omit when pushing a new version (use `slug` instead)."
          },
          "slug": {
            "type": "string",
            "description": "Slug of an existing doc to iterate (from a create_* result, `list_documents`, or a docstash URL — a full URL is accepted). OMIT for a NEW doc; a stale slug OVERWRITES that doc. Iterate-vs-new rules: server instructions."
          },
          "description": {
            "type": "string",
            "description": "One-line commit-style changelog for THIS version (≤~8 words — the CHANGE, not the whole doc), e.g. \"Add Q3 revenue table\". Set it on every call; it becomes the version-history entry."
          },
          "content": {
            "type": "string",
            "description": "One continuous, top-to-bottom HTML document you author and design; DocStash flows it across US Letter pages and paginates for you. Build designed set-pieces (cover, dividers, full-page infographics) as `<section class=\"pdf-canvas\">` — a FIXED one-page canvas where absolute positioning + full-bleed backgrounds are safe and content auto-fits the page — and let running text FLOW. `pdf-break` forces a page, `pdf-keep` keeps a block whole, `pdf-bleed` is a flowing full-bleed section; theme every page via `@page{background:…}`. Draw charts/graphics as inline SVG (static print doc — no JS). DocStash presents it as a real PDF."
          }
        },
        "required": [
          "content"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "create_docx",
      "description": "Generate an EDITABLE Word document (.docx) for DocStash and show the user an INLINE PREVIEW. Use this when the user specifically wants a WORD doc / editable .docx; for a DESIGNED, print-perfect document use `create_pdf` instead. `content` = JSON.stringify(DocxSpec) — the browser mints a real .docx from it at view/download time. Do NOT generate a binary. Spec schema below.\nThe spec expresses real Word fidelity: document font, named styles, page setup (size / orientation / margins / columns), headers & footers, a generated table of contents, per-run fonts / colors / sizes, and tables with merged / shaded cells. IMAGES & FONTS — image/font URLs MUST be DIRECT files (a gallery/viewer page renders BROKEN); the direct-URL recipes, sizing, and custom-font rules are in the server instructions (VISUAL DOCUMENTS & MEDIA).\n\nNothing is stashed yet — then STOP: do NOT call `stash` yourself; the user reviews the preview and saves it. Full lifecycle, slug/versioning, attribution + publish rules: DocStash server instructions.\n\nDOCX SPEC SHAPE — content = JSON.stringify(DocxSpec):\n{\n  title?, defaults?: { font?, size?(pt), color?(hex), lineSpacing? },\n  page?: { size?: 'A4'|'Letter'|'Legal', orientation?: 'portrait'|'landscape', margins?: {top,right,bottom,left}(inches), columns? },\n  header?: { content, align? }, footer?: { content, align? },   // OMIT both unless the user explicitly asks for a running header/footer — do NOT auto-add a title or tagline band; a bare document has none\n  toc?: true | { title?, maxLevel? },   // generated table of contents\n  styles?: [{ id, name?, font?, size?, color?, bold?, italic?, align?, spacing? }],   // reusable named paragraph styles\n  body: Block[]\n}\nBlock = one of:\n  { type:\"heading\", level:1-6, content, align? }\n  { type:\"paragraph\", content, align?, style?, spacing?:{before,after,line}, indent?:{left,firstLine,hanging} }\n  { type:\"list\", ordered?, items:[{ content, level? }] }\n  { type:\"table\", rows:[{ cells:[{ content, colSpan?, rowSpan?, align?, valign?, shading?(hex), bold? }], header? }], widths?(% per column), align?, cellPadding?(in), borderColor?(hex), borderWidth?(pt), banded?, borders? }\n  { type:\"image\", src(DIRECT image URL), alt?, width?, height?, align? }\n  { type:\"quote\", content } | { type:\"code\", text, language? } | { type:\"divider\" } | { type:\"pageBreak\" } | { type:\"toc\", title?, maxLevel? }\ncontent = a string, a run, or a list of either.\nRun = { text, bold?, italic?, underline?, strike?, code?, color?(hex), highlight?, font?, size?(pt), link?(url), superscript?, subscript? }.",
      "annotations": {
        "title": "Create Word doc",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Required when creating a new doc; omit when pushing a new version (use `slug` instead)."
          },
          "slug": {
            "type": "string",
            "description": "Slug of an existing doc to iterate (from a create_* result, `list_documents`, or a docstash URL — a full URL is accepted). OMIT for a NEW doc; a stale slug OVERWRITES that doc. Iterate-vs-new rules: server instructions."
          },
          "description": {
            "type": "string",
            "description": "One-line commit-style changelog for THIS version (≤~8 words — the CHANGE, not the whole doc), e.g. \"Add Q3 revenue table\". Set it on every call; it becomes the version-history entry."
          },
          "content": {
            "type": "string",
            "description": "JSON.stringify(DocxSpec). See the DOCX SPEC SHAPE in this tool description."
          }
        },
        "required": [
          "content"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "create_sheet",
      "description": "Generate a SPREADSHEET / WORKBOOK (Excel) for DocStash and show the user an INLINE PREVIEW. `content` = JSON.stringify(SheetSpec) — do NOT generate an .xlsx binary; the browser renders real Excel at view time. Spec schema below.\n\nNothing is stashed yet — then STOP: do NOT call `stash` yourself; the user reviews the preview and saves it. Full lifecycle, slug/versioning, attribution + publish rules: DocStash server instructions.\n\nXLSX SPEC SHAPE (when kind='xlsx'):\n  { theme?: '<preset>' | { custom theme }, sheets: [ Sheet, ... ] }   — 1-20 sheets per workbook. A custom theme is a FLAT object (fields at top level, NOT nested under `custom`): { headerBackground, headerText, text, stripe, border, accent, font? }.\n  Sheet: { name, rows: Row[], columnWidths?: number[], rowHeights?: number[], frozenRows?: number, frozenCols?: number, merges?: string[], autoFilter?: string, conditionalFormats?: CF[], charts?: Chart[] }\n    • name         — tab label, ≤31 chars; avoid [ ] : * ? / and \\\n    • rows         — top-to-bottom; each row is an array of cells (rows may be ragged — short rows pad empty on the right)\n    • columnWidths — left→right, Excel char-width units; omit to auto-fit\n    • rowHeights   — top→bottom, in points; omit/null an entry to keep default height\n    • frozenRows / frozenCols — sticky header rows / label columns that stay visible while scrolling\n    • merges       — A1 ranges to merge, e.g. ['A1:D1','A2:A5']; top-left cell's value/format wins (great for spanning titles)\n    • autoFilter   — A1 range to attach Excel filter dropdowns, e.g. 'A1:F1' (header) or 'A1:F100' (whole block)\n    • conditionalFormats — declarative rules (heatmaps, bars, icons, threshold highlights); see CF below\n  Cell — a BARE value for the common case, or an OBJECT when you need a formula / formatting / comment:\n    bare:   string | number | boolean | null\n    object: { value?, formula?, result?, format?, comment?, hyperlink?, validation? }\n      • formula   — Excel formula starting '=', e.g. '=SUM(B2:B10)', '=IF(A1>0,\"yes\",\"no\")', cross-sheet '=Sheet2!B5*1.1' (overrides value)\n      • result    — IMPORTANT cached formula value: ALWAYS supply the computed result alongside `formula`, else many viewers show the cell BLANK until they recalc. (You may instead pass `value` next to `formula` as the cache.)\n      • hyperlink — makes the cell a clickable link; its `value` is the display text\n      • validation — { type:'list'|'decimal'|'whole'|'date', values?, min?, max?, allowBlank?, prompt?, error? }; 'list'+values gives a dropdown\n  format (CellFormat): { bold?, italic?, underline?, strikethrough?, color?, background?, fontFamily?, align?: 'left'|'center'|'right', valign?: 'top'|'middle'|'bottom', numberFormat?, fontSize?, wrapText?, border? }\n    • color / background — hex '#RRGGBB' OR a theme token (color: 'text'|'accent';  background: 'headerBackground'|'stripe'|'accent')\n    • fontFamily   — per-cell font override (e.g. 'Arial','Courier New'); defaults to the theme font\n    • numberFormat — Excel format string: '#,##0' int · '#,##0.00' 2dp · '$#,##0' currency · '0.00%' percent · 'yyyy-mm-dd' date · '@' force-text\n    • border — per side { top?, right?, bottom?, left? }; each side is `true` (thin, theme color) OR { style?:'thin'|'medium'|'thick'|'double'|'dashed', color? }\n  CF (Sheet.conditionalFormats[]): { range:'B2:B100', type:'colorScale'|'dataBar'|'iconSet'|'cellIs'|'expression', ...rule }\n    • colorScale — colors?: 2-3 hex low→high (default red→yellow→green heatmap)\n    • dataBar    — color?: hex bar color (default theme accent)\n    • iconSet    — iconSet?: '3TrafficLights1'|'3Arrows'|'5Rating'|... (default '3TrafficLights1')\n    • cellIs     — operator:'greaterThan'|'lessThan'|'greaterThanOrEqual'|'lessThanOrEqual'|'equal'|'notEqual'|'between', value, value2?(for between), background?/fontColor?\n    • expression — formula:'$B2>$C2' (no leading '='), background?/fontColor? applied where TRUE\n  Chart (Sheet.charts[]): { type: 'bar'|'line'|'pie'|'area'|'scatter', title?, dataRange: 'A1:B10', categoriesRange?, position: { fromRow, fromCol, cols, rows } }  — rendered as a native Excel chart in the download.\n  CONVENTION — the FIRST row of each sheet is treated as the themed header. Put values/formulas as raw numbers (not pre-formatted strings) and let `numberFormat` style them, so totals/charts compute. For any `formula`, also pass its computed `result` so the workbook isn't blank on first open.\n  Theme presets:\n    • 'minimal' — Clean header bar (navy blue + white text), light gray stripes, sans-serif. Safe default for most data.\n    • 'corporate' — Deep blue header, white text, subtle stripes, Calibri. Standard business workbook look.\n    • 'finance' — Dark green header (currency-feel), white text, no stripes, Calibri. Financial models / accounting.\n    • 'research' — Charcoal header, off-white text, off-white stripes, Helvetica narrow. Academic / data-research vibe.\n    • 'minimal_dark' — Dark slate background throughout, light text, subtle border. Dark-mode tables.\n  Minimal example (JSON.stringify this): {\"theme\":\"finance\",\"sheets\":[{\"name\":\"Q3\",\"frozenRows\":1,\"rows\":[[\"Item\",\"Revenue\"],[\"Subscriptions\",{\"value\":42000,\"format\":{\"numberFormat\":\"$#,##0\"}}],[\"Total\",{\"formula\":\"=SUM(B2:B2)\",\"format\":{\"bold\":true,\"numberFormat\":\"$#,##0\"}}]]}]}",
      "annotations": {
        "title": "Create spreadsheet",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Required when creating a new doc; omit when pushing a new version (use `slug` instead)."
          },
          "slug": {
            "type": "string",
            "description": "Slug of an existing doc to iterate (from a create_* result, `list_documents`, or a docstash URL — a full URL is accepted). OMIT for a NEW doc; a stale slug OVERWRITES that doc. Iterate-vs-new rules: server instructions."
          },
          "description": {
            "type": "string",
            "description": "One-line commit-style changelog for THIS version (≤~8 words — the CHANGE, not the whole doc), e.g. \"Add Q3 revenue table\". Set it on every call; it becomes the version-history entry."
          },
          "content": {
            "type": "string",
            "description": "JSON.stringify(SheetSpec). See the SHEET SPEC SHAPE in this tool description."
          }
        },
        "required": [
          "content"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "create_app",
      "description": "Generate a LIVE WEB APP (a multi-file bundle) for DocStash and stage an UNSAVED VERSION the user reviews at a docstash.ai link. Use this ONLY when you're genuinely shipping MULTIPLE files (e.g. index.html + a separate app.js + styles.css + local assets). For a single self-contained page, use `create_page` instead — it has no bundle, so it can't fail on a missing local file.\nDeliver the finished app THROUGH this tool — DocStash hosts the bundle live at a shareable URL. Do NOT leave it as a local file/artifact or paste the files into the chat.\n\nPass `files: { \"path/to/file\": \"<contents>\", ... }`; the MCP server zips in-memory and uploads as a bundle. YOU MUST declare:\n  - `entry`    — the file the runtime mounts (e.g. 'index.html' for a static site, 'app.py' for a python script).\n  - `language` — the RUNTIME token: 'static' (HTML+CSS+JS), 'python', 'ruby', 'php', 'lua', 'go', 'rust', 'cpp', 'java', etc.\n  - `framework` — declare this WHENEVER the bundle uses any of these (lower-case, exact token): 'react' / 'vue' / 'svelte' / 'solid' / 'preact' / 'angular' / 'lit' / 'htmx' / 'alpine' / 'qwik' / 'astro' / 'three' (THREE.js / 3D animations / WebGL scenes) / 'p5' (p5.js / generative art / creative-coding) / 'd3' (D3.js / data viz / charts) / 'jquery'. Pick ONE — the primary library the app is built around. Drives the dashboard card chip.\n    Examples:\n      - HTML page imports Three.js to render a rotating cube → framework: 'three'\n      - React app with a useState counter                   → framework: 'react'\n      - p5.js sketch of bouncing particles                  → framework: 'p5'\n      - Plain HTML + CSS + a tiny <script> tag, no library  → omit `framework` entirely\n    Omit ONLY when the bundle uses NO known framework. Don't guess — you just generated the files, you know which library is in there. Realistic for agent-generated apps: typically 10-50 KB of text, fits in one tool call.\nBUILD-FREE RULE (language='static') — the bundle is served to the browser EXACTLY as uploaded; there is NO build step. Do NOT ship a framework SOURCE project: no `.svelte` / `.vue` / `.ts` / `.tsx` files, no Vite/webpack config expecting compilation (the browser can't load those as `<script type=\"module\">`, and DocStash won't compile them — the app fails with a 'module script … MIME text/html' error). For Svelte / React / Vue / Solid, either (a) inline a PREBUILT single bundle of plain .js/.css into `index.html`, or (b) import the framework from a CDN as ESM, e.g. `import { mount } from 'https://esm.sh/svelte'`. STRONGLY PREFER esm.sh — it serves correct, CORS-safe, versioned ESM, React included: `import React from 'https://esm.sh/react@18'` + `import { createRoot } from 'https://esm.sh/react-dom@18/client'`, mounted from a `<script type=module>`. AVOID the in-browser Babel transformer (@babel/standalone + a `text/babel` script): it's slow and the #1 runtime failure. And DON'T load React/ReactDOM from cdnjs UMD unless the path includes `/umd/` (…/react/18.2.0/umd/react.production.min.js) — omitting `umd/` 404s, the 404 HTML comes back as a non-executable script, and the app dies with 'React is not defined'.\nMODULE-FORMAT CHECKLIST (these three console errors mean you authored a BUILD-STEP/bundler app the runtime can't run) — (1) `Unexpected token 'export'` / (2) `Cannot use import statement outside a module`: a file using `import`/`export` (e.g. an esm.sh module or your own code) was loaded as a CLASSIC script. EVERY file that uses `import`/`export` MUST be loaded via `<script type=\"module\">`; a bare `<script src=…>` at an ESM module throws these. (3) `Unexpected token '<'`: a referenced local JS path (a build output like `/src/main.jsx`, `/assets/index-*.js`, or an extensionless `/client` entry) isn't in the bundle, so the static runtime SPA-falls-back and serves your index.html for that request — the browser then parses HTML as JS. NEVER reference build-output paths; only reference files you actually put in `files`, or absolute CDN URLs. If you'd hit any of these, re-author as EITHER a single self-contained page (prefer `create_page`) OR a bundle whose every script is inline or a `type=module` CDN-ESM import.\nSELF-CONTAINED BUNDLE RULE — the bundle must stand alone. Every LOCAL URL your entry references (scripts, styles, images, fonts) MUST be a key in `files`. For every asset you have exactly three valid choices: (a) inline it, (b) include it in `files`, or (c) load it from an absolute CDN URL (https://…). NEVER reference a local path you didn't write. In particular, do NOT add `<link rel=\"icon\" href=\"/favicon.ico\">` (or any icon/manifest link) unless you actually include that file — a stray local ref is the #1 cause of broken bundles. IMAGES & FONTS — image/font URLs MUST be DIRECT files (a gallery/viewer page renders BROKEN); the direct-URL recipes, sizing, and custom-font rules are in the server instructions (VISUAL DOCUMENTS & MEDIA).\n\nRESPONSIVE — the page MUST reflow + scroll on mobile: include the `<meta name=\"viewport\">` tag and DON'T lock it to `height:100vh` + `overflow:hidden` (that traps phone scrolling). The viewer renders full-width, no extra scroll container.\n\nNothing is stashed yet — then STOP: do NOT call `stash` yourself; the user reviews the unsaved version and saves it. Full lifecycle, slug/versioning, attribution + publish rules: DocStash server instructions.",
      "annotations": {
        "title": "Create web app",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Required when creating a new doc; omit when pushing a new version (use `slug` instead)."
          },
          "slug": {
            "type": "string",
            "description": "Slug of an existing doc to iterate (from a create_* result, `list_documents`, or a docstash URL — a full URL is accepted). OMIT for a NEW doc; a stale slug OVERWRITES that doc. Iterate-vs-new rules: server instructions."
          },
          "description": {
            "type": "string",
            "description": "One-line commit-style changelog for THIS version (≤~8 words — the CHANGE, not the whole doc), e.g. \"Add Q3 revenue table\". Set it on every call; it becomes the version-history entry."
          },
          "files": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "description": "Map of relative paths → UTF-8 file contents (e.g. `{ \"index.html\": \"<!doctype html>...\", \"styles.css\": \"body { ... }\" }`). The MCP server zips everything in memory and uploads as a bundle. Paths can't start with '/' or contain '..'. Must be SELF-CONTAINED — see the rule in this tool's description."
          },
          "entry": {
            "type": "string",
            "description": "The entry file inside the bundle the runtime mounts (e.g. 'index.html', 'app.py'). Must be a path that exists in `files`."
          },
          "language": {
            "type": "string",
            "description": "The RUNTIME token: 'static' (HTML+CSS+JS), 'python', 'ruby', 'php', 'lua', 'go', 'rust', 'cpp', 'java', etc."
          },
          "framework": {
            "type": "string",
            "description": "Optional. Declare a known framework when the bundle uses one: react / vue / svelte / solid / preact / angular / lit / htmx / alpine / qwik / astro / three / p5 / d3 / jquery. Lower-case. Drives the dashboard card chip. Omit for vanilla bundles."
          }
        },
        "required": [
          "files",
          "entry",
          "language"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "create_page",
      "description": "Generate a WEB PAGE (a single self-contained HTML file, rendered live) for DocStash and show the user an INLINE PREVIEW: landing pages, visual/interactive dashboards, infographics, demos — anything the user should SEE RENDERED rather than read as source. Content = a complete `<!doctype html>` document with everything inline (`<style>`, `<script>`) and any images/libraries referenced by absolute URL or CDN. IMAGES & FONTS — image/font URLs MUST be DIRECT files (a gallery/viewer page renders BROKEN); the direct-URL recipes, sizing, and custom-font rules are in the server instructions (VISUAL DOCUMENTS & MEDIA). Viewer provides chrome — don't wrap.\nDeliver the finished page THROUGH this tool — DocStash hosts it live at a shareable URL. Do NOT leave it as a local file/artifact or hand back raw HTML in the chat. Load any JS deps as `<script type=\"module\">` ESM (esm.sh) or inline them — never reference a local build-output path (the file won't exist).\nROUTING: ONE self-contained page → this tool, NOT `create_app` (a single file structurally can't have broken local references; use create_app only for genuinely MULTI-FILE bundles). Markdown / source code / raw data → `create_text` (inert source, not a rendered page). A designed PDF document → `create_pdf`; an editable Word doc → `create_docx`.\n\nRESPONSIVE — the page MUST reflow + scroll on mobile: include the `<meta name=\"viewport\">` tag and DON'T lock it to `height:100vh` + `overflow:hidden` (that traps phone scrolling). The viewer renders full-width, no extra scroll container.\n\nNothing is stashed yet — then STOP: do NOT call `stash` yourself; the user reviews the preview and saves it. Full lifecycle, slug/versioning, attribution + publish rules: DocStash server instructions.",
      "annotations": {
        "title": "Create web page",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Required when creating a new doc; omit when pushing a new version (use `slug` instead)."
          },
          "slug": {
            "type": "string",
            "description": "Slug of an existing doc to iterate (from a create_* result, `list_documents`, or a docstash URL — a full URL is accepted). OMIT for a NEW doc; a stale slug OVERWRITES that doc. Iterate-vs-new rules: server instructions."
          },
          "description": {
            "type": "string",
            "description": "One-line commit-style changelog for THIS version (≤~8 words — the CHANGE, not the whole doc), e.g. \"Add Q3 revenue table\". Set it on every call; it becomes the version-history entry."
          },
          "content": {
            "type": "string",
            "description": "A complete `<!doctype html>` document as a UTF-8 string, everything inline."
          }
        },
        "required": [
          "content"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "create_text",
      "description": "Generate an INERT text artifact — markdown, source code, or raw data — for DocStash and show the user an INLINE PREVIEW. This is for content the user READS AS SOURCE (typography for markdown, syntax highlighting for code), never executed or rendered as a page. Pick `type`:\n  • 'md'   — markdown. Content = the markdown source. For prose, notes, READMEs, technical docs, documentation of any kind.\n  • 'text' — source code / data in ~30 formats. Content = the raw source string. Set `language` to the canonical token (python / typescript / go / rust / json / yaml / csv / sql / etc.).\nROUTING: scripts, code files, READMEs, configs, raw CSV/JSON → here. A LIVE rendered page → `create_page`. A designed PDF (and any generic 'make me a document/PDF' ask) → `create_pdf`; an editable Word doc → `create_docx`. A spreadsheet with real cells/formulas → `create_sheet` (raw CSV data stays here).\n\nNothing is stashed yet — then STOP: do NOT call `stash` yourself; the user reviews the preview and saves it. Full lifecycle, slug/versioning, attribution + publish rules: DocStash server instructions.",
      "annotations": {
        "title": "Create text or code",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Required when creating a new doc; omit when pushing a new version (use `slug` instead)."
          },
          "slug": {
            "type": "string",
            "description": "Slug of an existing doc to iterate (from a create_* result, `list_documents`, or a docstash URL — a full URL is accepted). OMIT for a NEW doc; a stale slug OVERWRITES that doc. Iterate-vs-new rules: server instructions."
          },
          "description": {
            "type": "string",
            "description": "One-line commit-style changelog for THIS version (≤~8 words — the CHANGE, not the whole doc), e.g. \"Add Q3 revenue table\". Set it on every call; it becomes the version-history entry."
          },
          "type": {
            "type": "string",
            "enum": [
              "md",
              "text"
            ],
            "description": "'md' (markdown) or 'text' (source code / data)."
          },
          "content": {
            "type": "string",
            "description": "The content as a UTF-8 string: markdown source for 'md'; the raw source for 'text'."
          },
          "language": {
            "type": "string",
            "description": "For type='text', the canonical language token (python / json / csv / yaml / typescript / rust / etc.)."
          },
          "filename": {
            "type": "string",
            "description": "Optional hint for type='text' when `language` is omitted — server infers from the extension."
          }
        },
        "required": [
          "type",
          "content"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "edit_pdf",
      "description": "The DEFAULT way to change a PDF — edit it in place, or copy it into a new one. Emit ONLY the changes as `{ old_string → new_string }` pairs, not the whole document. `edit_pdf` edits only a PDF; a wrong-type call names the right tool. When to edit vs re-run a create tool, and iterate-vs-new-doc: DocStash server instructions.\n\n- `old_string` must be text that appears in the CURRENT source — call `read_document` first to get it, and copy it verbatim with enough surrounding context to match EXACTLY ONE place (or set `replaceAll`). Whitespace differences are tolerated. A stale or ambiguous `old_string` fails cleanly and changes NOTHING, so re-read and retry.\n- `new_string` replaces it; an empty string deletes the matched text.\n- Edits apply in order, each to the result of the previous.\n- **In-place** (default): the change lands as a new version of `slug`, shown as a fresh inline preview.\n- **Copy mode** (`copy: true` + `name`): leaves `slug` UNTOUCHED and produces a NEW doc — a filled-in copy with your edits applied. The TEMPLATE flow: keep one master, spin off variants (an invoice/offer-letter/report template → a copy per client). Returns the new doc's own slug.\n",
      "annotations": {
        "title": "Edit PDF",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The 6-char slug of the SOURCE doc (from `stash` / `list_documents` / URLs). In-place: this doc gets a new version. Copy mode: the template read from — it stays untouched."
          },
          "copy": {
            "type": "boolean",
            "description": "If true, DON'T change `slug` — create a NEW doc that is a copy of it with the edits applied (the template flow). Requires `name`."
          },
          "name": {
            "type": "string",
            "description": "Name for the NEW doc when `copy: true` (ignored for an in-place edit)."
          },
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "old_string": {
                  "type": "string",
                  "description": "EXACT text from the current source to replace — verbatim, with enough surrounding context to be unique. Whitespace differences are tolerated."
                },
                "new_string": {
                  "type": "string",
                  "description": "The replacement text. An empty string deletes the matched text."
                },
                "replaceAll": {
                  "type": "boolean",
                  "description": "Replace EVERY occurrence (default false — `old_string` must match exactly once)."
                }
              },
              "required": [
                "old_string",
                "new_string"
              ],
              "additionalProperties": false
            },
            "minItems": 1,
            "description": "Ordered surgical edits; each applies to the result of the previous one."
          },
          "description": {
            "type": "string",
            "description": "Optional one-line summary of the change, recorded on the new version."
          }
        },
        "required": [
          "slug",
          "edits"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "edit_docx",
      "description": "The DEFAULT way to change a Word (.docx) doc — edit it in place, or copy it into a new one. Emit ONLY the changes as `{ old_string → new_string }` pairs, not the whole document. `edit_docx` edits only a Word (.docx) doc; a wrong-type call names the right tool. When to edit vs re-run a create tool, and iterate-vs-new-doc: DocStash server instructions.\n\n- `old_string` must be text that appears in the CURRENT source — call `read_document` first to get it, and copy it verbatim with enough surrounding context to match EXACTLY ONE place (or set `replaceAll`). Whitespace differences are tolerated. A stale or ambiguous `old_string` fails cleanly and changes NOTHING, so re-read and retry.\n- `new_string` replaces it; an empty string deletes the matched text.\n- Edits apply in order, each to the result of the previous.\n- **In-place** (default): the change lands as a new version of `slug`, shown as a fresh inline preview.\n- **Copy mode** (`copy: true` + `name`): leaves `slug` UNTOUCHED and produces a NEW doc — a filled-in copy with your edits applied. The TEMPLATE flow: keep one master, spin off variants (an invoice/offer-letter/report template → a copy per client). Returns the new doc's own slug.\n- This doc is a JSON spec. For a VALUE change (a run's text, a heading, a cell, a hex color), match `old_string` on the VISIBLE text/value — NOT the raw JSON; you never escape quotes or backslashes, DocStash handles the JSON and re-validates.\n- To ADD structure, use `appendBlocks` (Block[] onto the body; optional `after` text anchor to place it) — no re-emit needed. Only a near-total rewrite, a reorder, or a removal re-emits via the create tool.\n",
      "annotations": {
        "title": "Edit Word doc",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The 6-char slug of the SOURCE doc (from `stash` / `list_documents` / URLs). In-place: this doc gets a new version. Copy mode: the template read from — it stays untouched."
          },
          "copy": {
            "type": "boolean",
            "description": "If true, DON'T change `slug` — create a NEW doc that is a copy of it with the edits applied (the template flow). Requires `name`."
          },
          "name": {
            "type": "string",
            "description": "Name for the NEW doc when `copy: true` (ignored for an in-place edit)."
          },
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "old_string": {
                  "type": "string",
                  "description": "EXACT text from the current source to replace — verbatim, with enough surrounding context to be unique. Whitespace differences are tolerated."
                },
                "new_string": {
                  "type": "string",
                  "description": "The replacement text. An empty string deletes the matched text."
                },
                "replaceAll": {
                  "type": "boolean",
                  "description": "Replace EVERY occurrence (default false — `old_string` must match exactly once)."
                }
              },
              "required": [
                "old_string",
                "new_string"
              ],
              "additionalProperties": false
            },
            "description": "Ordered surgical VALUE edits; each applies to the result of the previous. Optional when using a structural append op."
          },
          "appendBlocks": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": {}
            },
            "description": "STRUCTURAL add: Block[] appended to the document body (same block shapes as create_docx — heading / paragraph / table / list / image / …). Use to ADD content that old_string/new_string cannot."
          },
          "after": {
            "type": "string",
            "description": "Insert `appendBlocks` immediately AFTER the block whose VISIBLE text contains this string (e.g. a heading). Omit to append at the end of the doc."
          },
          "description": {
            "type": "string",
            "description": "Optional one-line summary of the change, recorded on the new version."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "edit_sheet",
      "description": "The DEFAULT way to change a spreadsheet — edit it in place, or copy it into a new one. Emit ONLY the changes as `{ old_string → new_string }` pairs, not the whole document. `edit_sheet` edits only a spreadsheet; a wrong-type call names the right tool. When to edit vs re-run a create tool, and iterate-vs-new-doc: DocStash server instructions.\n\n- `old_string` must be text that appears in the CURRENT source — call `read_document` first to get it, and copy it verbatim with enough surrounding context to match EXACTLY ONE place (or set `replaceAll`). Whitespace differences are tolerated. A stale or ambiguous `old_string` fails cleanly and changes NOTHING, so re-read and retry.\n- `new_string` replaces it; an empty string deletes the matched text.\n- Edits apply in order, each to the result of the previous.\n- **In-place** (default): the change lands as a new version of `slug`, shown as a fresh inline preview.\n- **Copy mode** (`copy: true` + `name`): leaves `slug` UNTOUCHED and produces a NEW doc — a filled-in copy with your edits applied. The TEMPLATE flow: keep one master, spin off variants (an invoice/offer-letter/report template → a copy per client). Returns the new doc's own slug.\n- This doc is a JSON spec. For a VALUE change (a run's text, a heading, a cell, a hex color), match `old_string` on the VISIBLE text/value — NOT the raw JSON; you never escape quotes or backslashes, DocStash handles the JSON and re-validates.\n- To ADD structure, use `addSheet` (a whole new sheet) or `addRows` (rows onto an existing sheet) — no re-emit needed. Only a near-total rewrite, a reorder, or a removal re-emits via the create tool.\n",
      "annotations": {
        "title": "Edit spreadsheet",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The 6-char slug of the SOURCE doc (from `stash` / `list_documents` / URLs). In-place: this doc gets a new version. Copy mode: the template read from — it stays untouched."
          },
          "copy": {
            "type": "boolean",
            "description": "If true, DON'T change `slug` — create a NEW doc that is a copy of it with the edits applied (the template flow). Requires `name`."
          },
          "name": {
            "type": "string",
            "description": "Name for the NEW doc when `copy: true` (ignored for an in-place edit)."
          },
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "old_string": {
                  "type": "string",
                  "description": "EXACT text from the current source to replace — verbatim, with enough surrounding context to be unique. Whitespace differences are tolerated."
                },
                "new_string": {
                  "type": "string",
                  "description": "The replacement text. An empty string deletes the matched text."
                },
                "replaceAll": {
                  "type": "boolean",
                  "description": "Replace EVERY occurrence (default false — `old_string` must match exactly once)."
                }
              },
              "required": [
                "old_string",
                "new_string"
              ],
              "additionalProperties": false
            },
            "description": "Ordered surgical VALUE edits; each applies to the result of the previous. Optional when using a structural append op."
          },
          "addSheet": {
            "type": "object",
            "additionalProperties": {},
            "description": "STRUCTURAL add: a whole new Sheet { name, rows: Row[][], … } appended to the workbook (same shape as a create_sheet sheets[] entry)."
          },
          "addRows": {
            "type": "object",
            "properties": {
              "sheet": {
                "type": [
                  "string",
                  "number"
                ],
                "description": "Target sheet by name or 0-based index; defaults to the last sheet."
              },
              "rows": {
                "type": "array",
                "items": {
                  "type": "array",
                  "items": {}
                },
                "description": "Row[][] to append (same cell shapes as create_sheet)."
              }
            },
            "required": [
              "rows"
            ],
            "additionalProperties": false,
            "description": "STRUCTURAL add: append rows to an existing sheet."
          },
          "description": {
            "type": "string",
            "description": "Optional one-line summary of the change, recorded on the new version."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "edit_page",
      "description": "The DEFAULT way to change a web page — edit it in place, or copy it into a new one. Emit ONLY the changes as `{ old_string → new_string }` pairs, not the whole document. `edit_page` edits only a web page; a wrong-type call names the right tool. When to edit vs re-run a create tool, and iterate-vs-new-doc: DocStash server instructions.\n\n- `old_string` must be text that appears in the CURRENT source — call `read_document` first to get it, and copy it verbatim with enough surrounding context to match EXACTLY ONE place (or set `replaceAll`). Whitespace differences are tolerated. A stale or ambiguous `old_string` fails cleanly and changes NOTHING, so re-read and retry.\n- `new_string` replaces it; an empty string deletes the matched text.\n- Edits apply in order, each to the result of the previous.\n- **In-place** (default): the change lands as a new version of `slug`, shown as a fresh inline preview.\n- **Copy mode** (`copy: true` + `name`): leaves `slug` UNTOUCHED and produces a NEW doc — a filled-in copy with your edits applied. The TEMPLATE flow: keep one master, spin off variants (an invoice/offer-letter/report template → a copy per client). Returns the new doc's own slug.\n",
      "annotations": {
        "title": "Edit web page",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The 6-char slug of the SOURCE doc (from `stash` / `list_documents` / URLs). In-place: this doc gets a new version. Copy mode: the template read from — it stays untouched."
          },
          "copy": {
            "type": "boolean",
            "description": "If true, DON'T change `slug` — create a NEW doc that is a copy of it with the edits applied (the template flow). Requires `name`."
          },
          "name": {
            "type": "string",
            "description": "Name for the NEW doc when `copy: true` (ignored for an in-place edit)."
          },
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "old_string": {
                  "type": "string",
                  "description": "EXACT text from the current source to replace — verbatim, with enough surrounding context to be unique. Whitespace differences are tolerated."
                },
                "new_string": {
                  "type": "string",
                  "description": "The replacement text. An empty string deletes the matched text."
                },
                "replaceAll": {
                  "type": "boolean",
                  "description": "Replace EVERY occurrence (default false — `old_string` must match exactly once)."
                }
              },
              "required": [
                "old_string",
                "new_string"
              ],
              "additionalProperties": false
            },
            "minItems": 1,
            "description": "Ordered surgical edits; each applies to the result of the previous one."
          },
          "description": {
            "type": "string",
            "description": "Optional one-line summary of the change, recorded on the new version."
          }
        },
        "required": [
          "slug",
          "edits"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "edit_text",
      "description": "The DEFAULT way to change a markdown / text / code doc — edit it in place, or copy it into a new one. Emit ONLY the changes as `{ old_string → new_string }` pairs, not the whole document. `edit_text` edits only a markdown / text / code doc; a wrong-type call names the right tool. When to edit vs re-run a create tool, and iterate-vs-new-doc: DocStash server instructions.\n\n- `old_string` must be text that appears in the CURRENT source — call `read_document` first to get it, and copy it verbatim with enough surrounding context to match EXACTLY ONE place (or set `replaceAll`). Whitespace differences are tolerated. A stale or ambiguous `old_string` fails cleanly and changes NOTHING, so re-read and retry.\n- `new_string` replaces it; an empty string deletes the matched text.\n- Edits apply in order, each to the result of the previous.\n- **In-place** (default): the change lands as a new version of `slug`, shown as a fresh inline preview.\n- **Copy mode** (`copy: true` + `name`): leaves `slug` UNTOUCHED and produces a NEW doc — a filled-in copy with your edits applied. The TEMPLATE flow: keep one master, spin off variants (an invoice/offer-letter/report template → a copy per client). Returns the new doc's own slug.\n",
      "annotations": {
        "title": "Edit text or code",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The 6-char slug of the SOURCE doc (from `stash` / `list_documents` / URLs). In-place: this doc gets a new version. Copy mode: the template read from — it stays untouched."
          },
          "copy": {
            "type": "boolean",
            "description": "If true, DON'T change `slug` — create a NEW doc that is a copy of it with the edits applied (the template flow). Requires `name`."
          },
          "name": {
            "type": "string",
            "description": "Name for the NEW doc when `copy: true` (ignored for an in-place edit)."
          },
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "old_string": {
                  "type": "string",
                  "description": "EXACT text from the current source to replace — verbatim, with enough surrounding context to be unique. Whitespace differences are tolerated."
                },
                "new_string": {
                  "type": "string",
                  "description": "The replacement text. An empty string deletes the matched text."
                },
                "replaceAll": {
                  "type": "boolean",
                  "description": "Replace EVERY occurrence (default false — `old_string` must match exactly once)."
                }
              },
              "required": [
                "old_string",
                "new_string"
              ],
              "additionalProperties": false
            },
            "minItems": 1,
            "description": "Ordered surgical edits; each applies to the result of the previous one."
          },
          "description": {
            "type": "string",
            "description": "Optional one-line summary of the change, recorded on the new version."
          }
        },
        "required": [
          "slug",
          "edits"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "edit_app",
      "description": "The DEFAULT way to change a multi-file app — edit it in place, or copy it into a new one. Emit ONLY the changes as `{ old_string → new_string }` pairs, not the whole document. `edit_app` edits only a multi-file app; a wrong-type call names the right tool. When to edit vs re-run a create tool, and iterate-vs-new-doc: DocStash server instructions.\n\n- `old_string` must be text that appears in the CURRENT source — call `read_document` first to get it, and copy it verbatim with enough surrounding context to match EXACTLY ONE place (or set `replaceAll`). Whitespace differences are tolerated. A stale or ambiguous `old_string` fails cleanly and changes NOTHING, so re-read and retry.\n- `new_string` replaces it; an empty string deletes the matched text.\n- Edits apply in order, each to the result of the previous.\n- **In-place** (default): the change lands as a new version of `slug`, staged as an UNSAVED VERSION the user reviews at its docstash.ai link (an app has no inline preview).\n- **Copy mode** (`copy: true` + `name`): leaves `slug` UNTOUCHED and produces a NEW doc — a filled-in copy with your edits applied. The TEMPLATE flow: keep one master, spin off variants (an invoice/offer-letter/report template → a copy per client). Returns the new doc's own slug.\n- Each edit names the `file` in the bundle to change; the str_replace applies within that file. Never re-emit a whole multi-file bundle for a small change. `read_document` with a `file` argument returns one file's current contents (a bare `read_document` lists the bundle's files).\n",
      "annotations": {
        "title": "Edit web app",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The 6-char slug of the SOURCE doc (from `stash` / `list_documents` / URLs). In-place: this doc gets a new version. Copy mode: the template read from — it stays untouched."
          },
          "copy": {
            "type": "boolean",
            "description": "If true, DON'T change `slug` — create a NEW doc that is a copy of it with the edits applied (the template flow). Requires `name`."
          },
          "name": {
            "type": "string",
            "description": "Name for the NEW doc when `copy: true` (ignored for an in-place edit)."
          },
          "edits": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "file": {
                  "type": "string",
                  "description": "The bundle file this edit targets (e.g. 'index.html', 'src/app.js'). Required."
                },
                "old_string": {
                  "type": "string",
                  "description": "EXACT text from the current source to replace — verbatim, with enough surrounding context to be unique. Whitespace differences are tolerated."
                },
                "new_string": {
                  "type": "string",
                  "description": "The replacement text. An empty string deletes the matched text."
                },
                "replaceAll": {
                  "type": "boolean",
                  "description": "Replace EVERY occurrence (default false — `old_string` must match exactly once)."
                }
              },
              "required": [
                "file",
                "old_string",
                "new_string"
              ],
              "additionalProperties": false
            },
            "minItems": 1,
            "description": "Ordered surgical edits; each applies to the result of the previous one."
          },
          "description": {
            "type": "string",
            "description": "Optional one-line summary of the change, recorded on the new version."
          }
        },
        "required": [
          "slug",
          "edits"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "stash",
      "description": "STASH a preview (from a create_* tool) — the actual SAVE; the doc stays PRIVATE. The `slug` IS the doc's slug (creates it, or pushes a new version if the preview targeted an existing doc). ‼️ Do NOT call this automatically after a create_* call — a plain 'create X' request is satisfied by the preview alone. Call it ONLY when the user signals saving (save / stash / ship it / looks good) or clicks Stash in the inline preview; when in doubt, ask first. Returns the `url`. Stashing does NOT publish — to make it public, then call `manage_sharing` action='set-public'.",
      "annotations": {
        "title": "Stash",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The slug returned by a create_* tool."
          },
          "name": {
            "type": "string",
            "description": "Optional override for the doc name at save time (new docs only)."
          },
          "description": {
            "type": "string",
            "description": "Optional one-line changelog OVERRIDE. Prefer setting this on the create_* tool instead — a changelog set here is lost if the user stashes the preview manually. Only use this to override the preview's stored changelog at save time."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "discard",
      "description": "Discard a preview (from a create_* tool) without stashing it. Call this when the user rejects it (e.g. 'discard', 'never mind', 'don't save it'). Nothing was persisted; this invalidates the preview so it can no longer be stashed.",
      "annotations": {
        "title": "Discard",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string",
            "description": "The slug returned by a create_* tool."
          }
        },
        "required": [
          "slug"
        ],
        "additionalProperties": true
      }
    },
    {
      "name": "get_organizations",
      "description": "List the organizations (personal + team) the user can save documents to, with the currently-active one flagged. Call this to resolve a spoken name like \"my team organization\" to its id before set_organization.",
      "annotations": {
        "title": "Get organizations",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": true
      }
    },
    {
      "name": "set_organization",
      "description": "Switch the active organization that NEW documents are created in, by organizationId (from get_organizations). Persists for this connection until changed. Existing documents remain reachable by slug regardless of the active organization.",
      "annotations": {
        "title": "Set organization",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "type": "object",
        "properties": {
          "organizationId": {
            "type": "string",
            "description": "The organization id from get_organizations."
          }
        },
        "required": [
          "organizationId"
        ],
        "additionalProperties": true
      }
    }
  ]
}
